<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Spring Boot + Lombok プロジェクト向け Checkstyle 設定 (Checkstyle 10.x)
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- タブ文字を禁止（スペースのみ使用） -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!-- 行末の空白を禁止 -->
    <module name="RegexpSingleline">
        <property name="format" value="\s+$"/>
        <property name="minimum" value="0"/>
        <property name="maximum" value="0"/>
        <property name="message" value="行末に空白があります。"/>
    </module>

    <!-- 1行の最大長: 120文字 -->
    <module name="LineLength">
        <property name="max" value="120"/>
        <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
    </module>

    <module name="TreeWalker">

        <!-- ======== インポート ======== -->
        <!-- 未使用インポートを禁止 -->
        <module name="UnusedImports"/>
        <!-- ワイルドカードインポートを禁止（staticインポートはOK） -->
        <module name="AvoidStarImport">
            <property name="allowStaticMemberImports" value="true"/>
        </module>

        <!-- ======== 命名規則 ======== -->
        <!-- クラス名: UpperCamelCase -->
        <module name="TypeName"/>
        <!-- メソッド名: lowerCamelCase -->
        <module name="MethodName"/>
        <!-- ローカル変数名: lowerCamelCase -->
        <module name="LocalVariableName"/>
        <!-- パラメータ名: lowerCamelCase -->
        <module name="ParameterName"/>
        <!-- フィールド名: lowerCamelCase -->
        <module name="MemberName"/>
        <!-- 定数名: UPPER_SNAKE_CASE（ロガー変数 log / LOG も許可） -->
        <module name="ConstantName">
            <property name="format" value="^(log|LOG|[A-Z][A-Z0-9]*(_[A-Z0-9]+)*)$"/>
        </module>
        <!-- パッケージ名: 小文字のみ -->
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>

        <!-- ======== インデント ======== -->
        <!-- インデント: 4スペース -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="caseIndent" value="4"/>
        </module>

        <!-- ======== ブロック ======== -->
        <!-- 空のブロックを禁止（コメントがあればOK） -->
        <module name="EmptyBlock">
            <property name="option" value="text"/>
        </module>
        <!-- 中括弧を必須に -->
        <module name="NeedBraces"/>
        <!-- 左中括弧の位置チェック -->
        <module name="LeftCurly"/>
        <!-- 右中括弧の位置チェック -->
        <module name="RightCurly"/>

        <!-- ======== コーディング ======== -->
        <!-- == を使った文字列比較を禁止 -->
        <module name="StringLiteralEquality"/>
        <!-- 簡素化できる boolean 式を禁止 -->
        <module name="SimplifyBooleanExpression"/>
        <!-- 配列宣言はJavaスタイル（int[] x, not int x[]） -->
        <module name="ArrayTypeStyle"/>
        <!-- long リテラルは L（大文字）を使用 -->
        <module name="UpperEll"/>
        <!-- switch の fall-through コメントを必須に -->
        <module name="FallThrough"/>

    </module>
</module>
